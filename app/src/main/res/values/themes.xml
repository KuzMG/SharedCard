<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SharedCard" parent="Theme.Material3.Light">
        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogTheme</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:textColor">@color/black</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="toolbarStyle">@style/toolbarStyle</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/textColorPrimary</item>
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button.TextButton.Dialog.Flush</item>
        <item name="radioButtonStyle">@style/Widget.App.RadioButton</item>
        <item name="android:radioButtonStyle">@style/Widget.App.RadioButton</item>
        <item name="fontFamily">@font/update_gothic</item>
    </style>

    <style name="Theme.SharedCard" parent="Base.Theme.SharedCard" />

    <style name="Theme.SharedCard.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
